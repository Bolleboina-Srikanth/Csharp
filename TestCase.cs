using Microsoft.VisualStudio.TestTools.UnitTesting;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using System.Text.RegularExpressions;
using System.Threading.Tasks;


namespace UnitTest1
{
  /*  [TestClass]
    public class TestCase
    {
        [TestMethod]
        public void validation()
        {
            RegularExpression r = new RegularExpression();
          // string phoneNumber = "^[6789][0-9]{9}";
            string result =( "8333035522");
          bool fg=  r.mobileNumber( result);
          //  string invalid = "25687869745";
          //  bool isvalid = Regex.IsMatch(phoneNumber, result);
           // bool isInvalid = Regex.IsMatch(invalid, result);
            Assert.IsTrue(fg);
            Assert.IsFalse(fg);
        }
    }*/
}
